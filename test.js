const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const p = await prisma.purchase.findFirst({ include: { items: true }, orderBy: { createdAt: 'desc' } }); console.log('Last Purchase:', p.invoiceNumber, 'Status:', p.deliveryStatus); console.log('Items:', p.items); const txs = await prisma.stockTransaction.findMany({ where: { referenceId: p.id } }); console.log('Stock Txns:', txs); } check().catch(console.error).finally(() => prisma.$disconnect());
